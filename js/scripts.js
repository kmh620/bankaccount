function Deposit(clientName, initialDeposit) {
  this.clientName = clientName,
  this.initialDeposit = initialDeposit
}

Deposit.prototype.newDeposit = function () {
   // add to inital deposit
};

Deposit.prototype.withdrawal = function () {
  /// subtract from initial dposit
}
